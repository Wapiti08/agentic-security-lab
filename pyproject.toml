[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agentic-security-lab"
version = "0.1.0"
description = "A lab for safe agentic security testing."
requires-python = ">=3.11"

